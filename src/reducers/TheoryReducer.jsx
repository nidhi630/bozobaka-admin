"use strict";

import * as Actions from "./../actions/ActionConstants";

let defaultState = {};

const TheoryReducer = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default TheoryReducer;